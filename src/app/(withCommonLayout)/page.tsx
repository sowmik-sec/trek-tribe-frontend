import { Typography } from "@mui/material";
import React from "react";

const HomePage = () => {
  return (
    <div>
      <Typography variant="h3">This is homepage</Typography>
    </div>
  );
};

export default HomePage;
